# Changelog

## 1.0.0 (initial)

- Hardened WebView with 9-layer content filter
- Google Sign-In + end-to-end encrypted Firestore sync
- Per-site timers, block list, white list, category toggles
- VPN-layer DNS sinkhole (opt-in)
- On-device NSFW image scoring via TFLite
- Boot-time watchdog + accessibility watchdog
- Encrypted Room DB (SQLCipher) + Tink AEAD for cloud payloads
